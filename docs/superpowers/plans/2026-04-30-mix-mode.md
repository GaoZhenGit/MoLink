# Mix 模式实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标:** 在 worker 端同端口实现 SOCKS5/HTTP 协议自动检测，UI 新增 MIX 选项作为第三模式并设为默认

**架构:** 新增 `MixProtocolDetector`（ByteToMessageDecoder）在 MIX 模式下 peek 首字节，0x05 路由到 SOCKS5 pipeline，其他路由到 HTTP pipeline。协议切换不重启服务，只需修改 `proxyType` 字段。ConnectionRecord 增加 protocol 字段用于日志区分。

**技术栈:** Java, Netty 4, Android 8.1+

**注意:** 实施过程中不自行 git commit，由用户决定何时提交。

---

### Task 1: ProxyProtocol 枚举增加 MIX 值，调整默认值

**文件:**
- 修改: `molink-worker/app/src/main/java/com/molink/worker/ProxyProtocol.java`

- [ ] **Step 1: 修改 ProxyProtocol.java**

```java
package com.molink.worker;

public enum ProxyProtocol {
    SOCKS5("SOCKS5"),
    HTTP("HTTP"),
    MIX("MIX");

    private final String displayName;

    ProxyProtocol(String displayName) {
        this.displayName = displayName;
    }

    public String getDisplayName() { return displayName; }

    public static ProxyProtocol fromString(String value) {
        if ("socks5".equalsIgnoreCase(value)) return SOCKS5;
        if ("http".equalsIgnoreCase(value)) return HTTP;
        return MIX;  // 默认 MIX（含 null）
    }
}
```

- [ ] **Step 2: 验证编译**

```
./gradlew :molink-worker:app:compileDebugJavaWithJavac
```

---

### Task 2: ConnectionRecord 增加 protocol 字段

**文件:**
- 修改: `molink-worker/app/src/main/java/com/molink/worker/ConnectionRecord.java`
- 修改: `molink-worker/app/src/main/java/com/molink/worker/netty/Socks5StateHandler.java`
- 修改: `molink-worker/app/src/main/java/com/molink/worker/netty/HttpProxyStateHandler.java`

- [ ] **Step 1: ConnectionRecord 新增 protocol 字段和构造函数参数**

```java
package com.molink.worker;

import java.util.concurrent.atomic.AtomicLong;

public class ConnectionRecord {

    public enum ConnectionState {
        ACTIVE,
        ENDED
    }

    public final String clientIp;
    public final String targetHost;
    public final int targetPort;
    public final long startTime;
    public final String protocol;
    public final AtomicLong bytesDown = new AtomicLong(0);
    public final AtomicLong bytesUp = new AtomicLong(0);
    public volatile ConnectionState state = ConnectionState.ACTIVE;

    public ConnectionRecord(String clientIp, String targetHost, int targetPort,
                            long startTime, String protocol) {
        this.clientIp = clientIp;
        this.targetHost = targetHost;
        this.targetPort = targetPort;
        this.startTime = startTime;
        this.protocol = protocol;
    }

    // ... 其余方法保持不变
}
```

- [ ] **Step 2: 更新 Socks5StateHandler 中的构造调用**

`Socks5StateHandler.java:257`，在 `ConnectionRecord` 构造末尾追加 `"SOCKS5"`:

```java
ConnectionRecord record = new ConnectionRecord(clientIp, targetHost, targetPort,
        System.currentTimeMillis(), "SOCKS5");
```

- [ ] **Step 3: 更新 HttpProxyStateHandler 中的构造调用**

`HttpProxyStateHandler.java:143`，在 `ConnectionRecord` 构造末尾追加 `"HTTP"`:

```java
ConnectionRecord record = new ConnectionRecord(clientIp, targetHost, targetPort,
        System.currentTimeMillis(), "HTTP");
```

- [ ] **Step 4: 验证编译**

```
./gradlew :molink-worker:app:compileDebugJavaWithJavac
```

---

### Task 3: 新增 MixProtocolDetector

**文件:**
- 创建: `molink-worker/app/src/main/java/com/molink/worker/netty/MixProtocolDetector.java`

- [ ] **Step 1: 编写 MixProtocolDetector**

```java
package com.molink.worker.netty;

import com.molink.worker.Socks5ProxyService;

import java.util.List;
import java.util.concurrent.TimeUnit;

import io.netty.buffer.ByteBuf;
import io.netty.channel.ChannelHandlerContext;
import io.netty.handler.codec.ByteToMessageDecoder;
import io.netty.handler.codec.http.HttpObjectAggregator;
import io.netty.handler.codec.http.HttpRequestDecoder;
import io.netty.handler.timeout.ReadTimeoutHandler;

/**
 * MIX 模式协议检测器。peek 首个字节：
 * 0x05 → SOCKS5 pipeline
 * 其他 → HTTP pipeline
 * 检测完成后移除自身和 readTimeoutHandler。
 */
public class MixProtocolDetector extends ByteToMessageDecoder {

    @Override
    protected void decode(ChannelHandlerContext ctx, ByteBuf in, List<Object> out) {
        if (in.readableBytes() < 1) return;

        byte firstByte = in.getByte(in.readerIndex());

        if (firstByte == 0x05) {
            ctx.pipeline().addLast(new Socks5StateHandler());
        } else {
            ctx.pipeline().addLast("httpDecoder", new HttpRequestDecoder());
            ctx.pipeline().addLast("httpAggregator", new HttpObjectAggregator(8192));
            ctx.pipeline().addLast(new HttpProxyStateHandler());
        }

        ctx.pipeline().remove("readTimeoutHandler");
        ctx.pipeline().remove(this);
    }
}
```

- [ ] **Step 2: 验证编译**

```
./gradlew :molink-worker:app:compileDebugJavaWithJavac
```

---

### Task 4: Socks5ProxyService 支持 MIX 模式

**文件:**
- 修改: `molink-worker/app/src/main/java/com/molink/worker/Socks5ProxyService.java`

- [ ] **Step 1: 修改 proxyType 默认值为 MIX**

第 58 行:
```java
private ProxyProtocol proxyType = ProxyProtocol.MIX;
```

- [ ] **Step 2: 修改 startServer() 增加 MIX 分支**

替换 `childHandler` 中的 `initChannel` 方法（第 217-230 行），增加 MIX 分支:

```java
.childHandler(new ChannelInitializer<Channel>() {
    @Override
    protected void initChannel(Channel ch) throws Exception {
        if (proxyType == ProxyProtocol.MIX) {
            ch.pipeline().addLast("readTimeoutHandler",
                    new io.netty.handler.timeout.ReadTimeoutHandler(5, java.util.concurrent.TimeUnit.SECONDS));
            ch.pipeline().addLast(new MixProtocolDetector());
        } else if (proxyType == ProxyProtocol.HTTP) {
            ch.pipeline().addLast(new HttpProxyStateHandler.ProtocolDetectingHandler());
            ch.pipeline().addLast("httpDecoder", new HttpRequestDecoder());
            ch.pipeline().addLast("httpAggregator", new HttpObjectAggregator(8192));
            ch.pipeline().addLast(new HttpProxyStateHandler());
        } else {
            ch.pipeline().addLast(new Socks5StateHandler());
        }
    }
});
```

- [ ] **Step 3: 新增 setProxyType() 方法**

在 `getProxyType()` 方法后添加:

```java
public void setProxyType(ProxyProtocol type) {
    this.proxyType = type;
    // 更新通知栏显示协议名
    updateNotification();
}
```

- [ ] **Step 4: 新增 updateNotification() 方法**

在 `createNotification()` 方法后添加，用于协议切换后更新通知栏:

```java
private void updateNotification() {
    NotificationManager manager = getSystemService(NotificationManager.class);
    manager.notify(NOTIFICATION_ID, createNotification());
}
```

- [ ] **Step 5: 验证编译**

```
./gradlew :molink-worker:app:compileDebugJavaWithJavac
```

---

### Task 5: UI 布局新增 MIX RadioButton

**文件:**
- 修改: `molink-worker/app/src/main/res/layout/activity_main.xml`

- [ ] **Step 1: 在 RadioGroup 中增加 MIX RadioButton，设为默认选中**

在 `activity_main.xml` 的 RadioGroup 中，httpRadio 之后添加:

```xml
<RadioButton
    android:id="@+id/mixRadio"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="MIX"
    android:textSize="14sp"
    android:checked="true"
    android:layout_marginStart="8dp"/>
```

同时将 `socks5Radio` 的 `android:checked="true"` 改为 `android:checked="false"`。

---

### Task 6: MainActivity 适配 MIX 模式，简化协议切换逻辑

**文件:**
- 修改: `molink-worker/app/src/main/java/com/molink/worker/MainActivity.java`

- [ ] **Step 1: 新增 mixRadio 字段**

在第 32 行，httpRadio 声明后添加:

```java
private RadioButton mixRadio;
```

- [ ] **Step 2: 移除 isSwitching 字段**

删除第 33 行:
```java
// 删除: private boolean isSwitching = false;
```

- [ ] **Step 3: findViewById 获取 mixRadio**

在 onCreate 中，httpRadio 引用后添加:

```java
mixRadio = findViewById(R.id.mixRadio);
```

- [ ] **Step 4: 替换 setOnCheckedChangeListener（简化切换逻辑）**

将第 85-108 行的监听器替换为:

```java
protocolRadioGroup.setOnCheckedChangeListener((group, checkedId) -> {
    Socks5ProxyService svc = Socks5ProxyService.getInstance();
    ProxyProtocol newType;
    if (checkedId == R.id.socks5Radio) {
        newType = ProxyProtocol.SOCKS5;
    } else if (checkedId == R.id.httpRadio) {
        newType = ProxyProtocol.HTTP;
    } else {
        newType = ProxyProtocol.MIX;
    }

    if (svc != null && svc.isRunning()) {
        svc.setProxyType(newType);
    }

    // 刷新 UI 显示
    if (svc != null && svc.isRunning()) {
        showRunning(svc);
    } else {
        showStopped();
    }
});
```

注意：移除 `isSwitching` 相关的 enable/disable 逻辑，RadioButton 始终可点击。

- [ ] **Step 5: 更新 getCurrentProxyTypeString() 支持 MIX**

第 139-143 行:

```java
private String getCurrentProxyTypeString() {
    int checkedId = protocolRadioGroup.getCheckedRadioButtonId();
    if (checkedId == R.id.httpRadio) return "http";
    if (checkedId == R.id.mixRadio) return "mix";
    return "socks5";
}
```

- [ ] **Step 6: 更新 showStopped() 支持 MIX 显示**

第 146-160 行，将固定协议判断改为动态获取:

```java
private void showStopped() {
    toggleButton.setText("启动服务");
    String displayName;
    int checkedId = protocolRadioGroup.getCheckedRadioButtonId();
    if (checkedId == R.id.httpRadio) displayName = "HTTP";
    else if (checkedId == R.id.mixRadio) displayName = "MIX";
    else displayName = "SOCKS5";
    statusRunning.setText(displayName + " 服务已停止");
    statusDot.setBackgroundResource(R.drawable.circle_gray);
    statusPort.setVisibility(View.GONE);
    statusUptime.setVisibility(View.GONE);
    connCount.setText("--");
    historyCount.setText("--");
    bytesDown.setText("--");
    bytesUp.setText("--");
    logAdapter.refreshAll(java.util.Collections.emptyList());
}
```

注意：移除 `socks5Radio.setEnabled(true)`、`httpRadio.setEnabled(true)`、`isSwitching = false`（不再需要）。

- [ ] **Step 7: 更新 showRunning() 支持 MIX 显示**

第 163-181 行:

```java
private void showRunning(Socks5ProxyService svc) {
    toggleButton.setText("停止服务");
    ProxyProtocol proto = svc != null ? svc.getProxyType() : getProxyTypeFromRadio();
    statusRunning.setText(proto.getDisplayName() + " 运行中");
    statusDot.setBackgroundResource(R.drawable.circle_green);
    statusPort.setVisibility(View.VISIBLE);
    statusUptime.setVisibility(View.VISIBLE);
    if (svc != null) {
        statusPort.setText("端口:" + svc.getPort());
        statusUptime.setText("在线:" + formatUptime(svc.getUptime()));
        connCount.setText(String.valueOf(svc.getConnectionCount()));
        // 根据实际协议同步 RadioGroup
        if (svc.getProxyType() == ProxyProtocol.HTTP) {
            protocolRadioGroup.check(R.id.httpRadio);
        } else if (svc.getProxyType() == ProxyProtocol.MIX) {
            protocolRadioGroup.check(R.id.mixRadio);
        } else {
            protocolRadioGroup.check(R.id.socks5Radio);
        }
    }
}
```

注意：移除 `socks5Radio.setEnabled(true)`、`httpRadio.setEnabled(true)`、`isSwitching = false`。

- [ ] **Step 8: 新增 getProxyTypeFromRadio() 辅助方法**

在 `getCurrentProxyTypeString()` 方法后添加:

```java
private ProxyProtocol getProxyTypeFromRadio() {
    int checkedId = protocolRadioGroup.getCheckedRadioButtonId();
    if (checkedId == R.id.httpRadio) return ProxyProtocol.HTTP;
    if (checkedId == R.id.mixRadio) return ProxyProtocol.MIX;
    return ProxyProtocol.SOCKS5;
}
```

- [ ] **Step 9: 验证编译**

```
./gradlew :molink-worker:app:compileDebugJavaWithJavac
```

---

### Task 7: 连接日志列表项显示协议标签

**文件:**
- 修改: `molink-worker/app/src/main/res/layout/item_connection_log.xml`
- 修改: `molink-worker/app/src/main/java/com/molink/worker/ConnectionLogAdapter.java`

- [ ] **Step 1: item_connection_log.xml 第一行增加协议标签**

在 `connDot` 和 `connTargetHost` 之间插入协议 TextView:

```xml
<!-- 协议标签 -->
<TextView
    android:id="@+id/connProtocol"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="SOCKS5"
    android:textSize="10sp"
    android:textColor="#FFFFFF"
    android:background="#4CAF50"
    android:paddingHorizontal="4dp"
    android:paddingVertical="1dp"
    android:layout_marginStart="6dp"/>
```

- [ ] **Step 2: ViewHolder 新增 protocol TextView 字段**

在 `ConnectionLogAdapter.java` 的 ViewHolder 中，第 78-80 行后添加字段声明:

```java
private final TextView protocol;
```

并在构造函数中获取引用:

```java
protocol = itemView.findViewById(R.id.connProtocol);
```

- [ ] **Step 3: bind() 方法中绑定协议标签**

在 `bind()` 方法开始处添加（第 90-91 行后）:

```java
protocol.setText(record.protocol);
if ("SOCKS5".equals(record.protocol)) {
    protocol.setBackgroundColor(0xFF2196F3);  // 蓝色
} else {
    protocol.setBackgroundColor(0xFFE91E63);  // 粉色
}
```

- [ ] **Step 4: 验证编译**

```
./gradlew :molink-worker:app:compileDebugJavaWithJavac
```

---

## 验证清单

所有 Task 完成后：

- [ ] 完整构建: `./gradlew :molink-worker:app:assembleDebug`
- [ ] UI 验证: RadioGroup 显示三个选项（SOCKS5 / HTTP / MIX），MIX 默认选中
- [ ] 协议切换: 运行中切换三种协议，已有连接不断，新连接正确识别
- [ ] 大文件传输: MIX 模式下 HTTP/SOCKS5 客户端持续传输 >30 秒
- [ ] 连接日志: 列表中每条连接显示蓝色 SOCKS5 或粉色 HTTP 标签
- [ ] 运行测试脚本: `python test\test.py`
